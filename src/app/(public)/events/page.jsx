import React from 'react';
import PublicEvents from '../../../components/public/PublicEvents';
export const metadata = { title: 'Campus Events', description: 'Upcoming approved events across all 12 ZP schools.' };
export default function EventsPage() { return <PublicEvents />; }
